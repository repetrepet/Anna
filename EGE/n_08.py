from itertools import product

# ==============================================================================
# ЗАДАЧА 1: Кодовый замок (Поиск количества перестановок с условием)
# ==============================================================================
# Шифр кодового замка представляет собой последовательность из пяти символов, 
# каждый из которых является цифрой от 1 до 4. Сколько различных вариантов 
# шифра можно задать, если известно, что цифра 1 встречается ровно два раза, 
# а каждая из других допустимых цифр может встречаться в шифре любое 
# количество раз или не встречаться совсем?

alph = '1234' # Алфавит допустимых символов (цифры от 1 до 4)
l = []        # Список для сохранения подходящих вариантов (можно и просто счетчик использовать)

# product(alph, repeat=5) генерирует все возможные комбинации длиной 5
# Это аналог 5 вложенных циклов for
for i in product(alph, repeat=5):
    # i — это кортеж символов, например ('1', '2', '1', '4', '3')
    
    # Метод .count('1') считает количество единиц в кортеже
    if i.count('1') == 2:
        l.append(i) # Если условие выполнено, добавляем вариант в список

print(f"Задача 1: {len(l)}")


# ==============================================================================
# ЗАДАЧА 2: Четверичная система (Числа и арифметические условия)
# ==============================================================================
# Сколько существует различных трёхзначных чисел, записанных в четверичной 
# системе счисления, в записи которых сумма первой и последней цифры строго 
# больше цифры стоящей по середине?

aplh = '0123' # Алфавит четверичной системы
l = []

# Генерируем все возможные тройки цифр
for i in product(aplh, repeat=3):
    # i[0] — первая цифра, i[1] — вторая, i[2] — третья
    
    # Условие 1: i[0] != '0'. Число не может начинаться с нуля.
    # Условие 2: Сумма крайних > средней.
    # Важно: элементы i — это строки ('0', '1'...), поэтому для математики 
    # их нужно преобразовать в числа через int()
    if (i[0] != '0') and (int(i[0]) + int(i[2]) > int(i[1])):
        l.append(i)

print(f"Задача 2: {len(l)}")


# ==============================================================================
# ЗАДАЧА 3: Семеричная система (Убывающий порядок цифр)
# ==============================================================================
# Сколько существует различных четырёхзначных чисел, записанных в семеричной 
# системе счисления, в записи которых цифры следуют слева направо в строго 
# убывающем порядке?

aplhabet = '0123456' # Алфавит семеричной системы
l = []

# Генерируем 4-значные комбинации
for i in product(aplhabet, repeat=4):
    # Числа должны строго убывать: 1-е > 2-го > 3-го > 4-го.
    # В Python строки сравниваются лексикографически, что для цифр 0-9 
    # совпадает с числовым сравнением (т.е. '5' > '4' дает True).
    # Здесь не нужно проверять i[0] != '0', так как при строгом убывании
    # ноль никогда не окажется на первом месте (меньше нуля в алфавите ничего нет).
    if i[0] > i[1] > i[2] > i[3]:
        l.append(i)

print(f"Задача 3: {len(l)}")


# ==============================================================================
# ЗАДАЧА 4: Слова из букв (Ограничения на буквы)
# ==============================================================================
# Составляют 5-буквенные слова из букв слова ПЯТНИЦА. Найти количество слов, 
# которые не начинаются с Н и в которых есть только одна буква Я. 
# Буквы в слове могут повторяться.

count = 0 # Используем простой счетчик вместо списка

# repeat=5, так как слова 5-буквенные
for i in product('ПЯТНИЦА', repeat=5):
    # i[0] != 'Н' — слово не начинается на Н
    # i.count('Я') == 1 — буква Я встречается ровно один раз
    if i[0] != 'Н' and i.count('Я') == 1:
        count += 1

print(f"Задача 4: {count}")


# ==============================================================================
# ЗАДАЧА 5: Сложное условие ("МАНГУСТ")
# ==============================================================================
# Алиса составляет 6-буквенные слова из букв М, А, Н, Г, У, С, Т. Каждая из 
# букв может встречаться сколько угодно раз, причём первой буквой не может 
# быть А, буква У должна встречаться не менее 1 раза. Также в записи должны 
# быть ровно две буквы М. Сколько различных слов может составить Алиса?

count = 0
for i in product('МАНГУСТ', repeat=6):
    # Комбинируем три условия через логическое И (and):
    # 1. i[0] != 'А' — не начинается на А
    # 2. i.count('У') >= 1 — есть хотя бы одна У
    # 3. i.count('М') == 2 — ровно две буквы М
    if i[0] != 'А' and i.count('У') >= 1 and i.count('М') == 2:
        count += 1

print(f"Задача 5: {count}")


# ==============================================================================
# ЗАДАЧА 6: Алфавитный порядок и нумерация слов
# ==============================================================================
# Все 5-буквенные слова, в составе которых могут быть буквы А, Л, Г, О, Р, И, Т, М, 
# записаны в алфавитном порядке и пронумерованы начиная с 1.
# ...
# Определите в этом списке количество слов с нечетными номерами, которые не 
# начинаются с буквы Г и при этом содержат в своей записи не менее двух букв И.

count = 0  # Количество подходящих слов
number = 1 # Номер текущего слова в списке (начинается с 1)

# ВАЖНО: Для задач с порядком слов алфавит ОБЯЗАТЕЛЬНО нужно сортировать.
# sorted('АГИЛМОРТ') гарантирует, что перебор пойдет: ААААА, ААААГ, ААААИ...
for i in product(sorted('АГИЛМОРТ'), repeat=5):
    
    # number % 2 != 0 — проверяем, что номер слова нечетный
    # i[0] != 'Г' — не начинается на Г
    # i.count('И') >= 2 — содержит 2 или более букв И
    if number % 2 != 0 and i[0] != 'Г' and i.count('И') >= 2:
        count += 1
    
    number += 1 # Увеличиваем номер слова на каждой итерации цикла
  
print(f"Задача 6: {count}")


# ==============================================================================
# ЗАДАЧА 7: Девятеричная система и запрещенные пары ("Соседи")
# ==============================================================================
# Игорь составляет пятизначные числа, используя цифры девятеричной системы 
# счисления. Сколько различных чисел может составить Игорь, в которых только 
# одна цифра 5 и рядом с ней НЕ стоят нечётные цифры?

count = 0

# Список запрещенных пар. В 9-ичной системе (0..8) нечетные цифры: 1, 3, 5, 7.
# Так как '5' у нас всего одна, она не может стоять рядом с другой '5'.
# Значит, запрещены пары '5' с '1', '3', '7'.
# Проверяем как '5' слева от нечетной, так и '5' справа от нечетной.
t = ['51','53', '57', '15', '35', '75']

for i in product('012345678', repeat=5):
    p = ''.join(i) # Склеиваем кортеж в строку для удобного поиска подстрок
    
    # 1. p[0] != '0' — число не начинается с нуля
    # 2. p.count('5') == 1 — ровно одна пятерка
    # 3. all(not x in p for x in t) — ни одна пара из списка t не содержится в числе p
    # (переменная цикла в all названа x, чтобы не путать с i из product)
    if p[0] != '0' and p.count('5') == 1 and all(not x in p for x in t):
        count += 1

print(f"Задача 7: {count}")

# all(...) — это встроенная функция Python, которая проверяет, что все элементы итерируемого объекта истинны (то есть дают True).

# Базовая идея
# all([True, True, True]) → True

# all([True, False, True]) → False

# all([]) → True (важный нюанс: “для пустого набора нет контрпримеров”, поэтому считается истинным)

# То есть all работает как логическое “И” по списку условий.

# Как all работает в задаче 7
# Фрагмент: all(not x in p for x in t)
# Здесь:
# t = ['51','53','57','15','35','75'] — список запрещённых соседних пар

# p — строка из 5 цифр, например "20458"

# Выражение внутри all — это генератор:

# (not x in p for x in t)
# Он по очереди подставляет каждую строку x из списка t и проверяет:

# есть ли подстрока x в строке p

# затем делает отрицание not

# То есть для каждого x получается условие:

# not ('51' in p)

# not ('53' in p)

# ...

# not ('75' in p)

# А all(...) требует, чтобы все эти условия были истинны. Иначе говоря: “в числе не встречается ни одна запрещённая пара”.

# Маленький пример “вручную”
# Пусть:

# p = "20578"

# Проверяем пары из t:

# '51' in p → False, значит not False → True

# '53' in p → False → True

# '57' in p → True (в строке есть "57"), значит not True → False

# Как только появляется False, итог:

# all(...) → False
# И число не подходит, потому что рядом с 5 стоит нечётная 7.

# Важное свойство: “короткое замыкание”
# all не обязан проверять всё до конца. Он работает так:

# встретил False → сразу возвращает False и прекращает проверки

# Это ускоряет перебор, потому что плохие варианты отсеиваются быстрее.

# Эквивалент без all (чтобы было совсем понятно)
# Вот то же самое, но “обычным кодом”:

# python
# ok = True
# for x in t:
#     if x in p:      # нашли запрещённую пару
#         ok = False
#         break       # дальше проверять смысла нет

# if ok:
#     count += 1
# То есть all(not x in p for x in t) — это компактная форма проверки “ни одной запрещённой подстроки нет”.


from itertools import product, permutations

# ==================================================================================
# ЗАДАЧА 1: Поиск слова по номеру (product)
# ==================================================================================
# Все 5-буквенные слова, составленные из букв А, О, У, записаны в алфавитном порядке.
# Вот начало списка:
# 1.ААААА
# 2.ААААО
# 3.ААААУ
# 4.АААОА
# Запишите слово, которое стоит на 210-м месте от начала списка.

# 1. Формируем алфавит. Важно: буквы должны быть в алфавитном порядке,
# так как product генерирует комбинации, опираясь на порядок во входной строке.
alph = 'АОУ'

# 2. Перебираем все варианты.
# product(alph, repeat=5) создает декартово произведение (размещения с повторениями).
# Это аналог 5 вложенных циклов.
# enumerate нумерует элементы, начиная с 0 (по умолчанию).
for number, i in enumerate(product(alph, repeat=5)):
    # Нам нужно 210-е слово.
    # Так как нумерация в enumerate начинается с 0, то:
    # 1-е слово имеет индекс 0
    # 210-е слово имеет индекс 209
    if number == 209:
        print(f"Задача 1: {i}") # Выведет кортеж букв ('А', 'О', 'А', 'А', 'У') и т.п.


# ==================================================================================
# ЗАДАЧА 2: Поиск слова по номеру со сдвигом (product)
# ==================================================================================
# Все 4-буквенные слова, составленные из букв К, Л, Р, Т,
# записаны в алфавитном порядке и пронумерованы.
# Вот начало списка:
# 1.КККК
# 2.КККЛ
# 3.КККР
# 4.КККТ
# Запишите слово, которое стоит под номером 67.

alph = 'КЛРТ' # Алфавит уже упорядочен

for number, i in enumerate(product(alph, repeat=4)):
    # Нам нужен номер 67.
    # Индекс компьютера = Человеческий номер - 1
    if number == 67 - 1:
        print(f"Задача 2: {i}")


# ==================================================================================
# ЗАДАЧА 3: Поиск номера первого слова по условию (product + break)
# ==================================================================================
# Все 5-буквенные слова, составленные из букв А, К, Р, У, записаны в алфавитном порядке.
# Укажите номер первого слова, которое начинается с буквы К.

alph = 'АКРУ'

for number, i in enumerate(product(alph, repeat=5)):
    # i — это кортеж, например ('А', 'А', 'А', 'А', 'А').
    # i[0] — это первая буква слова.
    if i[0] == 'К':
        # Выводим number + 1, так как в задаче нумерация с 1, а number с 0.
        print(f"Задача 3: {number + 1}")
        break # Прерываем цикл, так как нужно только ПЕРВОЕ такое слово.


# ==================================================================================
# ЗАДАЧА 4: Поиск номера конкретного слова (product)
# ==================================================================================
# Все 5-буквенные слова, составленные из букв А, К, Р, У, записаны в алфавитном порядке.
# Укажите номер слова УКАРА.

alph = 'АКРУ'

for number, i in enumerate(product(alph, repeat=5)):
    # Сравниваем текущий сгенерированный кортеж с искомым словом.
    if i == ('У', 'К', 'А', 'Р', 'А'):
        # Снова корректируем индекс (+1), чтобы получить человеческий номер.
        print(f"Задача 4: {number + 1}")


# ==================================================================================
# ЗАДАЧА 5: Перестановки букв без повторений (permutations)
# ==================================================================================
# Виктор составляет коды из букв, входящих в слово ВИКТОР.
# Каждая буква должна входить в код ровно один раз.
# Все возможные коды Виктор записывает в алфавитном порядке и нумерует.
# Какой код будет записан под номером 266?

# Используем permutations, так как буквы не должны повторяться (перестановки).
# Важно отсортировать алфавит вручную, чтобы перебор шел в алфавитном порядке:
# В, И, К, О, Р, Т (О идет перед Р в алфавите).
alph = 'ВИКОРТ'

for index, i in enumerate(permutations(alph)):
    # Ищем 266-й элемент, значит индекс равен 265.
    if index == 266 - 1:
        print(f"Задача 5: {i}")


# ==================================================================================
# ЗАДАЧА 6: Сложные условия фильтрации
# ==================================================================================
# Все пятибуквенные слова, составленные из букв С, Т, Р, О, К, А,
# записаны в алфавитном порядке и пронумерованы.
# Определите, под каким номером в этом списке стоит последнее слово
# с чётным номером, которое не начинается с букв А, С или Т
# и при этом содержит в своей записи ровно две буквы О.

# 1. Сортируем алфавит: А, К, О, Р, С, Т
alph = 'АКОРСТ'

for index, i in enumerate(product(alph, repeat=5)):
    # Условие 1: Чётный номер слова.
    # Если номер слова (человеческий) четный (2, 4, 6...),
    # то индекс (компьютерный) будет нечетным (1, 3, 5...).
    # Пример: 2-е слово -> index 1.
    condition_even_number = (index % 2 != 0)
    
    # Условие 2: Не начинается с А, С или Т.
    condition_start = (i[0] not in 'АСТ')
    
    # Условие 3: Ровно две буквы О.
    condition_count = (i.count('О') == 2)

    if condition_even_number and condition_start and condition_count:
        # Так как цикл идет по возрастанию, программа будет печатать все подходящие варианты.
        # Последнее напечатанное число в консоли и будет ответом (так как это последнее слово).
        print(f"Задача 6 (кандидат): {index + 1}")

# 06.05.2026

# Все 5-буквенные слова, составленные из букв A, B, T, O, Б, У, С, записаны в алфавитном порядке и пронумерованы. Вот начало списка:
# ААААА
# ААААБ
# ААААВ
# ААААО
# ААААС
# ААААТ
# ААААУ
# АААБА
# ...
# Под каким номером в списке стоит первое слово, в котором нет A, O и повторяющихся букв и которое заканчивается на СБ?


from itertools import permutations, product

# for i in product('abc', repeat=3): # перебор всех варинатов
#     print(i)
# Пояснение: product() формирует декартово произведение (вложенные циклы в одну строку).
# repeat=3 указывает длину генерируемых последовательностей.

# for i in permutations('abc'): # перестановка
#     print(i)
# Пояснение: permutations() генерирует все уникальные перестановки без повторений.
# Порядок элементов учитывается, длина равна длине исходной последовательности.

alph = 'абвосту'
number = 1

for i in product(alph, repeat=5):
    if ('а' not in i) and ('о' not in i):
        if len(i) == len(set(i)):
            if i[-1] == 'б' and i[-2] == 'с':
                print(number)
                break
    number += 1
# Пояснение к решению:
# 1. product() автоматически генерирует комбинации в лексикографическом порядке,
#    если входной алфавит отсортирован, что позволяет сразу находить порядковый номер.
# 2. ('а' not in i) and ('о' not in i) — фильтрация: буквы "а" и "о" не должны встречаться.
# 3. len(i) == len(set(i)) — проверка на отсутствие повторов символов в слове.
#    set(i) оставляет только уникальные элементы, их количество сравнивается с исходной длиной.
# 4. i[-1] == 'б' and i[-2] == 'с' — условие на окончание слова: предпоследний символ "с", последний "б".
# 5. number считает позицию в переборе. При нахождении первого подходящего слова выводим номер и прерываем цикл (break).


# Все пятибуквенные слова, в составе которых могут быть только русские буквы К, О, Ф, Е,
# записаны в алфавитном порядке и пронумерованы начиная с 1. Ниже приведено начало списка.
#
# ЕЕЕЕЕ
# ЕЕЕЕК
# ЕЕЕЕО
# ЕЕЕЕФ
# ЕЕЕКЕ ...
# Определите сумму номеров первого и последнего слов в списке, в которых только одна буква О и
# при этом никакая согласная буква не стоит рядом с буквой О.

from itertools import product

# Алфавит в алфавитном порядке: е, к, о, ф — именно в таком порядке product будет генерировать слова
alph = 'екоф'
number = 1  # Счётчик номера текущего слова в лексикографическом списке
l = []  # Список для хранения номеров слов, удовлетворяющих условиям задачи

# Генерируем все возможные пятибуквенные комбинации из букв алфавита в алфавитном порядке
# product(alph, repeat=5) создаёт декартово произведение, что эквивалентно 5 вложенным циклам
for i in product(alph, repeat=5):
    word = ''.join(i)  # Преобразуем кортеж из 5 символов в строку-слово

    # Первое условие: в слове должна быть ровно одна буква 'о'
    if word.count('о') == 1:
        # Второе условие: никакая согласная (к или ф) не должна стоять рядом с 'о'
        # Проверяем отсутствие запрещённых пар символов: 'ко', 'ок', 'фо', 'оф'
        # Функция all() возвращает True, если все элементы итерируемого объекта истинны
        if all(x not in word for x in ['ко', 'ок', 'фо', 'оф']):
        # Альтернативная запись того же условия через явные проверки (эквивалентна строке выше):
        # if ('ко' not in word) and ('ок' not in word) and ('фо' not in word) and ('оф' not in word):
            l.append(number)  # Если оба условия выполнены — сохраняем номер слова

    number += 1  # Увеличиваем счётчик для следующего слова

# Выводим сумму номеров первого и последнего подходящих слов из найденного списка
print(l[0] + l[-1])
